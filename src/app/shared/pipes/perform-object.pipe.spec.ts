import { PerformObjectPipe } from './perform-object.pipe';

describe('PerformObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new PerformObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
